services:
  postgres:
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_PASSWORD=1
    volumes:
      - ./mnt/postgres:/var/lib/postgresql/data
  api:
    build: .
    restart: always
    ports:
      - 2727
